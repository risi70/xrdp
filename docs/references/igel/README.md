# igel

IGEL OS 12 endpoint, RDP client, smartcard, and UMS profile references.

See `../manifest.yaml` for source URLs and retrieval settings.

`originals/` and `converted/` are intentionally ignored by git.
